import { ExecuteDirective } from './execute.directive';

describe('ExecuteDirective', () => {
  it('should create an instance', () => {
    const directive = new ExecuteDirective();
    expect(directive).toBeTruthy();
  });
});
